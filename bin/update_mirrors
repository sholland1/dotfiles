#!/bin/bash
cp /etc/pacman.d/mirrorlist /etc/pacman.d/mirrorlist.backup
URL='https://archlinux.org/mirrorlist/?country=US&protocol=http&protocol=https&ip_version=4&use_mirror_status=on'
curl -s "$URL" |
    sed -e 's/^#Server/Server/;/^#/d' |
    rankmirrors -n 6 - > /etc/pacman.d/mirrorlist
