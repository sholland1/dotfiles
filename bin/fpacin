#!/bin/bash
RESULT=($(pacman -Slq |
    fzf --multi --preview 'pacman -Si {}' \
        --preview-window=:75%))

if [ $? -eq 0 ]; then
    sudo pacman -S "${RESULT[@]}"
    if [ $? -eq 0 ]; then
        read -n 1 -s -r -p 'Press any key to exit...'
    fi
fi
